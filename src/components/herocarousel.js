export function createHeroCarousel(slides) {
  // slides = [{ type: 'video', src: '...' }, { type: 'image', src: '...' }, ...]
  // Génère le HTML du carousel
}

export function initHeroCarousel(selector) {
  // Initialise le swipe, auto-play, dots, vidéos, etc.
}